export function PageHeader({eyebrow,title,description,action}:{eyebrow:string;title:string;description:string;action?:React.ReactNode}) { return <div className="mb-8 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="mb-2 text-[10px] font-bold uppercase tracking-[.2em] text-leaf">{eyebrow}</p><h1 className="text-3xl font-bold tracking-[-.04em] text-ink md:text-4xl">{title}</h1><p className="mt-2 max-w-xl text-sm text-[#718079]">{description}</p></div>{action}</div> }
